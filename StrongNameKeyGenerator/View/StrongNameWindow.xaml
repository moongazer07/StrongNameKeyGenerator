<Window x:Class="StrongNameKeyGenerator.View.StrongNameWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" Title="Generate StrongName Key" ResizeMode="NoResize">
    <Grid>       
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="textBoxStrongNameKey" Height="22" Margin="6,4" Text="{Binding StrongKey}" VerticalAlignment="Center" Width="280" Grid.ColumnSpan="2"/>

        <Button Content="Ok" Height="23" Margin="0,4" Name="Button1" Width="66" Command="{Binding OkCommand}" Grid.Row="1" HorizontalAlignment="Right" Grid.Column="1" />
    </Grid>
</Window>
